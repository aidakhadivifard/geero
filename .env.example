# Copy to `.env`. Read only by the Express server — never sent to the browser.

# Required for real card generation (https://console.anthropic.com/).
ANTHROPIC_API_KEY=

# Optional. Server port (default 8787).
PORT=8787

# Optional. "1" serves curated offline demo cards when no API key is set.
DAWNHALO_ALLOW_FALLBACK=0
