// Dawn gradient palette (product doc §7). Single source of truth shared by the
// SVG illustrations; the CSS mirrors these as custom properties.
export const palette = {
  night: "#2E2430", // deep plum — card surfaces & illustration backgrounds
  cream: "#FBF6EE", // warm cream — app backgrounds/surfaces
  gold: "#E8B872", // primary accent / "halo"
  peach: "#F2A989", // secondary accent
  nightSoft: "#3C3040",
  goldSoft: "#F0CE98",
};
